version: "3"

services:
  react-nginx:
    container_name: react-nginx
    build:
      context: .
      dockerfile: frontend.Dockerfile
    volumes:
      - .:/app
      - /app/nodes_modules
      # - /app/nginx/nginx.conf
    ports:
      - "3000:80"
    # depends_on:
    #   - web
