version: "3"

services:
  app:
    container_name: react-nginx
    build:
      context: .
      dockerfile: frontend.Dockerfile
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "3000:80"
    # depends_on:
    #   - db
    #   - web
